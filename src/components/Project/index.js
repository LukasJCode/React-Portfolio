import "./index.scss";
import Carousel from "../Carousel";

const Bubbles = (props) =>{

    return (
        <div>
            <Carousel
                id={1}
            />
        </div>
    )
}

export default Bubbles;